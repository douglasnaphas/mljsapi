const roomCode = require('./room-code');

describe('room-code', () => {
  test('Room Code should be six letters', () => {});
  test('Room Codes from two calls should be different', () => {});
  test('Room Codes should ', () => {});
});