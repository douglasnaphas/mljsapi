const rejoin = [
  // if a valid game name cookie was sent, log (for now) and return/send
  
  // set db params to get the session key
  
  // set cookie
  
  // success, send
];
module.exports = rejoin;