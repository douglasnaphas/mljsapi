const setDbParamsGetEmailFromSub = (local) => {
  if(typeof local !== "string") {
    throw "setDbParamsGetEmailFromSub: non-string local";
  }
  const middleware = (req, res, next) => {
    
  };
  return middleware;
};
module.exports = setDbParamsGetEmailFromSub;